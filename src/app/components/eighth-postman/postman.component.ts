import { Component } from '@angular/core';
import { HttpClient } from '@angular/common/http';

@Component({
    selector: 'postman',
    templateUrl: './postman.component.html',
})
export class PostmanComponent {

    // constructor(private http: HttpClient) { }

    // private apiUrl = 'http://example.com/api/';

    // getItems() {
    //     return this.http.get(this.apiUrl + 'items');
    // }

    // addItem(item) {
    //     return this.http.post(this.apiUrl + 'items', item);
    // }

    // updateItem(id, item) {
    //     return this.http.put(this.apiUrl + 'items/' + id, item);
    // }

    // partialUpdateItem(id, item) {
    //     return this.http.patch(this.apiUrl + 'items/' + id, item);
    // }

    // deleteItem(id) {
    //     return this.http.delete(this.apiUrl + 'items/' + id);
    // }

    //   this.itemService.getItems().subscribe(data => {
    //     this.items = data;
    //   });
}


